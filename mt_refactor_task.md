# 重构 Charging Pose Estimator
该项目实现了一个实时运动的相机对静态目标的位姿估计。  
彻底重构项目，采用生产者-消费者多线程模型，通过 `Queue` 实现线程间数据传递（通信），将项目拆分为：
- 数据读入线程
- 模型推导（检测）线程
- match(圆心refine)线程
- 后端优化线程
- 可视化和数据保存线程

## 数据读入线程

编写数据管理类，实现可处理离线数据集或在线传感器数据：  

### 离线数据集读取

以 `dataset/0515` 为例，其中包含：图片 + 机械臂位姿 .npy。  

**要求：**  
- 可适应 `.png/.jpg` 等多种格式图片
- 检查图片和位姿文件名（时间戳）是否相同，可先以 `dataset/0515` 数据集格式为准
- 预留数据集格式接口，以后可针对不同格式数据集，扩展方法

### 在线传感器数据读取

包含：相机 + 机械臂位姿  

**要求：**  
- 传感器 API 接口和写法可参考 `chargeport_pose_estimator_ba.py`
- 读取频率可配置
- 完成相机图片和机械臂位姿的时间同步

**总要求：**  
- 使用对应模式时，只需初始化各自相关参数
- 设置合理接口，将 图片 + 机械臂位姿传出，供其他线程使用(使用Queue?)  

## 模型推导线程
可参考 `trt_pose_inf.py` 和 `getInferResults`, 用类来管理模型，给出推导（检测）接口。  

**要求：**  
- 输入数据：图片（来自于数据读入线程给出的）
- 输出：roi区域和关键点 `keypoints`

## match(圆心refine)线程
基于关键点 `keypoints` 和 图片，精确化圆心，输出处理后的圆心坐标。
**要求：**
- 采用多线程版本，多点同时处理，可参考 `_match_socket_mt()` 中写法
- 输入：图片和关键点（来自模型推导线程）
- 输出：处理的圆心

## 优化线程
match(圆心refine)线程后，圆心和对应的机械臂位姿应输出（合并插入到queue?），优化器线程读入，输出优化后的位姿  
参考 `chargeport_pose_estimator_ba.py` 和 `static_pose_optimizer_ba.py`
**要求：**  
- 输入：圆心（pts2d）+ 机械臂位姿(robot pose)
- 输出：该线程计算得到的优化位姿即为最终结果，合理处理输出形式
- 修改 `static_pose_optimizer_ba.py` ，类的初始化必须输入object_points，每帧读入时不需给出 pts3d，若存在 pts2d 个数与 pts3d 个数不一致的情况，应在输入的 pts2d 缺省位置上合理标记，或以某种合理形式传递给优化过程，后续优化计算基于此来处理
- 合理设置类的初始化接口，区分常需配置和保持默认配置的参数，避免接口冗余

## 可视化和数据保存线程
可视化内容，图片保存，数据可视化分析等内容与 `chargeport_pose_estimator_ba.py` 基本保持一致
**要求：**  
- 必须有结果数据的可视化，并保存图片
- 可配置是否开启各类图片的保存
- 数据保存默认开启，保存内容可在原版基础上调整，去除冗余部分


## 其他要求
- 合理处理用于各线程间通信的 `queue` 的弹出和插入
- 设置日志等级，合理打印和存储日志
- 可配置是否开启可视化
- 需要有可配置是否开启的程序性能分析部分，统计关键模块的耗时

## 编写和调试要求
- 在新文件上开发
- 在 conda 环境 `yolov8-trt` 下测试
- 使用数据集 `dataset/0515`测试离线模式