This repository contains an initial refactor scaffold for the charging pose estimator.

Run (offline dataset) after installing requirements:

```bash
pip install -r requirements.txt
python main.py
```

Structure follows the pipeline described in mt_refactor_task.md.
